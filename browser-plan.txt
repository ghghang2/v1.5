# Browser tool enhancement plan

1. **Add extraction helpers** – Implement `_elements`, `extract`, `evaluate`, and `wait_for` in `BrowserManager`. 
   
2. **Add new wrapper actions** – Extend the `browser` function to support `wait_for`, `extract`, `evaluate`, and `screenshot` with a `full_page` flag.
   
3. **Update schema** – Include the new actions in the OpenAI function‑calling schema.
   
4. **Validate** – Run quick manual tests to ensure navigation, extraction, evaluation, and screenshot work.
   
5. **Document** – Add comments describing the new actions.
   
6. **Mark tasks complete** – Update this file with a ✅ next to each completed task.

---

✅ 1. Add extraction helpers
✅ 2. Add new wrapper actions
✅ 3. Update schema
✅ 4. Validate
✅ 5. Document
✅ 6. Mark tasks complete
