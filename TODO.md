# TODO

This file tracks the progress of the refactor to move hard‑coded
configuration values into ``repo_config.yaml``.

## Steps

1. **Identify hard‑coded values** in the ``nbchat`` package.
2. **Move each value** to ``repo_config.yaml``.
3. **Update imports** to read from ``nbchat.core.config``.
4. **Write tests** to verify that values are correctly loaded.
5. **Document inefficiencies** encountered during the refactor.

---

Current status: Pending.
